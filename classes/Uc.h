#include <string>
#include <list>

using namespace std;

class Uc{
    private:
        string ucCode;
         // if parsing through all students classes takes too long, create a list of students
    public:
}